// replace with src for images
const slideImages = [1, 2, 3, 4, 5];
var imageSrc = slideImages[slideNum];
var slideNum = 0;

function prevImg() {
  slideNum--;
}

function nextImg() {
  slideNum++;
}
